{
  "name": "text-compare",
  "version": "1.0.0",
  "description": "A text-compare.com-style two-text diff comparison tool (fully client-side, built for Cloudflare Pages)",
  "private": true,
  "scripts": {
    "dev": "http-server public -p 8080 -c-1",
    "deploy": "wrangler pages deploy public --project-name text-compare"
  },
  "license": "MIT"
}
